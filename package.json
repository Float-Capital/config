{
  "name": "float-config",
  "version": "0.1.0",
  "scripts": {
    "res:build": "rescript build -with-deps",
    "res:start": "rescript build -w -with-deps",
    "res:clean": "rescript clean -with-deps",
    "res:format": "rescript format -all",
    "refmt": "rescript format -all"
  },
  "description": "Smart contract addresses for Float Capital",
  "main": "index.js",
  "repository": "https://github.com/Float-Capital/config",
  "author": "Float DAO",
  "license": "MIT",
  "keywords": [
    "bucklescript",
    "rescript"
  ],
  "dependencies": {
    "rescript": "^9.1.4"
  }
}
